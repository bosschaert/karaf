h1. Synopsis

${project.name}

${project.description}

Maven URL:
[mvn:${project.groupId}/${project.artifactId}/${project.version}]

h1. Description

This bundle adds role-based security to OSGi services, allowing only callers with the right roles to invoke the service.